# 安装内核模块命令

**insmod**：

原始加载器（开发调试用）

**depmod**：

构建依赖数据库

depmod 默认不是固定更新 /lib/modules 下某一个目录，而是更新当前运行内核对应的模块目录。使用uname -r查看当前内核版本名称。

相当于depmod $(uname -r)

**modprobe：**

 生产级加载器

**rmmod：**

卸载模块

**没有 depmod 时：**

modprobe → 找不到 modules.dep → 无法解析依赖
# 编译设备树

**查看当前linux内核使用的设备树（默认是**imx6ull-14x14-emmc-4.3-480x272-c.dts改成别的也被刷掉，暂未解决**）：**

```bash
# 查看根节点的 compatible 属性，这会显示板子的兼容性字符串
cat /proc/device-tree/compatible | tr '\0' '\n'
# 查看根节点的 model 属性
cat /proc/device-tree/model | tr '\0' '\n'
```

**使用uboot打印当前设备树：**

=> print fdt_file
fdt_file=imx6ull-14x14-emmc-4.3-480x272-c.dtb

或

=> printenv

**使用uboot修改设备树：**

=> setenv fdt_file imx6ull-14x14-emmc-4.3-800x480-c.dtb
=> saveenv # 保存到环境变量存储区（如MMC、Flash）
=> boot

**救砖：**

setenv fdt_file imx6ull-14x14-evk-emmc.dtb

**编译设备树：**

export CROSS_COMPILE=/usr/local/arm/gcc-linaro-4.9.4-2017.01-x86_64_arm-linux-gnueabihf/bin/arm-linux-gnueabihf-
export ARCH=arm

**设备树文件在内核源码：**

cd arch/arm/boot/dts

**在dts目录下：**

make -C ../../../../ dtbs

**在内核路径下：**

make dtbs 或者
注意：若遇到权限问题，则可以chmod -R u+w /home/zhangxin/workspace_imx6ull/kernel

**拷贝：**

cp imx6ull-14x14-emmc-4.3-480x272-c.dtb temp/
cp -v imx6ull-14x14-emmc-4.3-480x272-c.dtb /home/zhangxin/linuxShare/

**拷贝SCP到emmc**

scp imx6ull-14x14-emmc-4.3-480x272-c.dtb root@192.168.55.71:/run/media/mmcblk1p1/

**切换到boot文件夹：**

cd /run/media/mmcblk1p1

**打印内核信息：**

dmesg | grep "failed to enable bus clock"

**列出当前的设备节点：**ls /proc/device-tree**查看设备节点：**for f in *do echo "---- $f ----" hexdump -C "$f"done

**救砖（开发板输入）：**cp /run/media/mmcblk0p1/imx6ull-14x14-emmc-4.3-480x272-c_bak.dtb /run/media/mmcblk0p1/imx6ull-14x14-emmc-4.3-480x272-c.dtb**反编译：**进入kernel/scripts/dtc路径./dtc -I dtb -O dts imx6ull-14x14-emmc-4.3-480x272-c.dtb -o imx6ull-14x14-emmc-4.3-480x272-c.dts

**找phandle的最大值：**grep phandle imx6ull-14x14-emmc-4.3-480x272-c.dts当前最大值0x4e因此保险起见使用phandle = <0x50>;